<h1>Produtos</h1>

<%=render 'search'%>

<hr />

<table>
  <tr>
    <th>Código</th>
    <th>Descricao</th>
    <th>Grupo</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @produtos.each do |produto| %>
  <tr>
    <td><%= produto.id %></td>
    <td><%= produto.descricao %></td>
    <td><%= produto.grupo.descricao %></td>
    <td><%= link_to 'Show', grupo_path(produto.grupo,produto	) %></td>
    <td><%= link_to 'Edit', edit_grupo_path(produto.grupo,produto) %></td>
    <td><%= link_to 'Destroy', grupo_path(produto.grupo,produto), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>



<br />

<%= link_to 'New Produto', new_grupo_path %>
