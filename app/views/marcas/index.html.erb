<h1>Marcas</h1>

<%= render 'search' %>

<hr />

<table>
  <tr>
    <th>Código</th>
    <th>Marca</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @marcas.each do |marca| %>
  <tr>
    <td><%= marca.id %></td>
    <td><%= marca.nome %></td>
    <td><%= link_to 'Show', marca %></td>
    <td><%= link_to 'Edit', edit_marca_path(marca) %></td>
    <td><%= link_to 'Destroy', marca, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<%= will_paginate @marcas %>	

<br />

<%= link_to 'Nova Marca', new_marca_path %>
